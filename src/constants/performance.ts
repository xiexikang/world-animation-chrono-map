/** 地球初始展示的海报标记数 */
export const DEFAULT_GLOBE_MARKERS = 100

/** 地球标记硬上限（性能保护） */
export const MAX_GLOBE_MARKERS_CAP = 500

/** @deprecated 使用 DEFAULT_GLOBE_MARKERS；保留别名避免旧引用报错 */
export const MAX_GLOBE_MARKERS = DEFAULT_GLOBE_MARKERS

/** 右侧国家面板每页作品数 */
export const PANEL_NODES_PAGE_SIZE = 100
